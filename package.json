{
  "name": "dc-ignite-ui",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel"
  },
  "devDependencies": {
    "tsup": "^8.0.2",
    "turbo": "^1.13.2",
    "typescript": "^5.4.5"
  }
}
